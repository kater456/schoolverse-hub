// Legacy profile page - not used in current marketplace routes
const Profile = () => {
  return <div className="p-8 text-center text-muted-foreground">Profile page moved.</div>;
};

export default Profile;
